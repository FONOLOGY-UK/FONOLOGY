{
  "name": "@fonology/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Real-browser end-to-end tests against a DEPLOYED Fonology (staging). Writes real data, then removes it. Never run against production — it refuses to.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:install": "playwright install chromium",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.13.1",
    "typescript": "^5.7.3"
  }
}
