import { ScaffoldNotice } from '@/components/shared/scaffold-notice';

/** Homepage — exists in the prototype; reproduced verbatim in Phase 2. */
export default function HomePage() {
  return (
    <ScaffoldNotice surface="Storefront" title="Home" phase="Phase 2 — storefront reproduction" />
  );
}
